Drop your background track here as  bg.mp3

The music button in the footer stays hidden until a playable file is found,
so an empty folder never leaves a dead control on the page.

Point config.js -> footer.music at a different filename if you prefer.
Keep it under ~3 MB and start it on a soft note; it fades in over 1.1s.
